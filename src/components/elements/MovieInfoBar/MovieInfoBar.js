import React from 'react';
import FontAwesome from 'react-fontawesome';
import { calcTime, convertMoney } from '../../../helpers';
import './MovieInfoBar';

const MovieInfoBar = (pros) => {
  <div>
    MovieInfoBar
  </div>
}

export default MovieInfoBar;